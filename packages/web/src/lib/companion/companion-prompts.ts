/** Forwarding module — the implementation lives in @read-pal/shared
 * (M4b) so mobile can consume the same genre templates once D1 unfreezes.
 * Import paths are unchanged for all existing consumers. */

export * from '@read-pal/shared/src/companion-prompts';
